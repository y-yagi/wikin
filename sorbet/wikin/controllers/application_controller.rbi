# FIXME: depend on auto generated files
class ActionController::Base
  extend T::Sig

  def self.http_basic_authenticate_with(name:, password:, realm: T.unsafe(nil)); end

  def self.layout(name); end

  def request; end
end
