  <div class="navbar navbar-default navbar-static-top">
    <div class="container">
      <div class="navbar-header">
        <a class="navbar-brand" href="<%= root_path %>">Wikin!</a>
      </div>
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">ページ操作<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="<%= new_page_path %>">新規作成</a></li>
            <% if @page.present? && @page.id.present?%>
              <li><%= link_to '編集', edit_page_path(@page) %></li>
              <li><%= link_to '削除', @page, method: :delete, data: { confirm: 'ページを削除します。宜しいですか?' } %></li>
            <% end %>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">ちょっと考える</a>
        </li>
      </ul>
      <span class="navbar-text navbar-right">
        <%= form_tag search_index_path, class: "form-inline", method: :get do |_| %>
          <%= text_field '', :q, class: "form-control", placeholder: "サイト内検索", style: "width: 400px" %>
          <%= submit_tag '検索', class: "btn btn-default" %>
        <% end %>
      </span>
     </div>
  </div>
